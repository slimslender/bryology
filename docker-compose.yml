version: '3'
services:
  mongo:
    image: mongo:3.6
  web:
    build: .
    ports:
     - "8080:8080"
    environment:
     - MONGODBURI=mongodb://mongo:27017/test 
    links:
     - mongo 
    dependsOn: 
     - mongo 
    volumes:
     - .:/starter
     - /starter/nodeModules
     